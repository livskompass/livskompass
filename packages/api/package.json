{
  "name": "@livskompass/api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute livskompass-db --local --file=./schema.sql",
    "db:migrate:prod": "wrangler d1 execute livskompass-db --remote --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "nanoid": "^5.0.0",
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.65.0"
  }
}
